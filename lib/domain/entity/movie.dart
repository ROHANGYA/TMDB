class Movie {
  int id;
  String title;
  String backdropPath;
  String posterPath;

  Movie(
      {required this.id,
      required this.title,
      required this.backdropPath,
      required this.posterPath});
}
