import 'package:tmdb/data/data_sources/remote/api_client_provider.dart';

class MovieApi extends ApiClientProvider {
  // get featured movies function and return to repository impl.
  // This is a simple api call in a try/catch (catch [NetworkExceptions]
}
